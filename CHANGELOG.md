# NoethingScript 版本历史

> 当前版本见 [README.md](README.md)。本文件按时间倒序记录各版本变更。

- **2.7.3**：模块系统 M1–M6 与运行前参数 cmdargs（设计稿 `module-system/design.md` v0.9 实现；实际实现与设计偏差见 `module-system/implementation-deviations.md`）——**模块系统**：头部 `modules ... endmodules` 块 + `use [inner] 模块名 [from 来源] [as 别名]` 声明式激活（来源仅 `main`/`extra`/`custom` 三分类，默认 `main`），按 `{模块目录}/{来源}/{包}/{模块}/{模块}.ns` 定位（命令行 `--modules DIR` 配置目录，默认 `modules/`；浏览器经 `NSI.setModuleDir` 注入）；模块函数统一走 `call 对象.函数()` 点分调用并编译进 NSVM（CALLFUNC 支持模块命名空间寻址，点分调用无行解释器回退）；模块符号挂模块命名空间对象、不进全局函数表，模块函数内查找链为局部变量 → 模块私有全局 → 未声明报错，与主程序零冲突，交互唯一通道为"调用 + 返回值"；`autoinit ... endautoinit` 块在被 use 时执行，模块顶层语句被 use 时不执行；模块输出/未捕获错误强制来源标识 `[模块 来源/包/模块]`；use 嵌套依赖递归加载、模块实例跨文件幂等、循环依赖检测；`use inner` 按当前文件路径上两级目录定位包根（浏览器经 `NSI.setCurrentFilePath` 注入）；浏览器加载能力经 `NSI.setModuleLoader` 注入（Node 默认 fs 读 `{MODULE_DIR}/{name}`）；加载期错误（use 失败/模块语法错误/依赖循环）整体终止、不可 `try-catch`（模块函数内错误与普通函数一致、主程序可捕获）；新增 12 个模块/参数体系关键字为保留字；**运行前参数 (cmdargs)**：头部 `cmdargs` 块 + `param 名:类型 = 默认值 [as "短名"]`（`string`/`bool`/`cast.int`/`cast.float`，默认值必须字面量），命令行 `--` 分隔符后脚本参数区按声明匹配——`--名字 值`/`-短名 值`/匿名按序填充/bool 开关翻转（不吞后面值）/cast 按 `int()`/`float()` 语义转换；未知有名参数/缺值/转换失败/孤立 `--` 报错，多余匿名丢弃警告；param 绑定为只读全局（修改报错与 const 一致），**先于 autoinit 与正文执行**；只对入口文件生效（被 use 的模块写 cmdargs 不报错、不生效、参数保持默认值）；浏览器等价入口 `NSI.setCmdargs(args)`；**其他**：CLI 未知参数提示补全 `--modules`；文档同步（doc.md 补 cmdargs/模块系统章节，README 补特性与目录说明）；新增 `tests/cmdargs_tests.ns` 与 `tests/cmdargs_syntax_error.ns`，全部 26 测试逐字节回归一致（M7 模块管理器未实现，待后续）

- **2.7.2**：String.* 白话化命名 + 类型系统收紧（数字/字符串/运算符）+ 数组元素类型三档语义（均属破坏性变更，2.7 系列不稳定期攒批）——**数字类型只收数字值**：`number`/`int`/`float` 收紧为仅接受 `typeof === 'number'` 的值，移除字符串与布尔的隐式强转（此前 `Number("3.9")=3.9`、`Number(false)=0`，导致 `n = String.findFirst(...)` 找不到时静默变 `0` 与下标 0 无法区分，且 bool 能被 `number` 接受与 int/float 行为不一致）；现在字符串（含 `"3.9"`）与布尔赋值一律报类型错误，需数字请用 `int(x)`/`float(x)` 显式转换（转换函数独立走 `parseInt`/`parseFloat`，不受影响）；`number` 与 `float` 同时拒收非有限值 `NaN`/`Infinity`（如 `Math.sqrt(-1)` 赋给数字变量报错），NSVM 三处赋值快速路径（标量赋值/数组元素写入 ×2）与 `validateType` 对齐——此前 NSVM 快速路径 FLOAT/NUMBER 直接收任意 `number`，`NaN`/`Infinity` 可绕过类型检查与行解释器行为不一致，现已逐分支对齐（`validateType` 单点收紧、双路径零分叉）；**字符串类型只收字符串值**：`string` 赋值收紧为仅接受 `typeof === 'string'`（此前 `st = 42` 静默变 `"42"`、`st = true` 变 `"true"`），数字/布尔赋值报错需 `str(x)` 显式转换；**运算符无隐式转换**：`+` 仅允许 `数字+数字`（加法）或 `字符串+字符串`（拼接），混合类型（`"a" + 5`、`5 + true`、`true + "a"` 等，此前静默 JS 强转）一律报类型错误；一元 `+`/`-` 仅接受数字、`!` 仅接受布尔（此前 `+"5"`、`!0` 静默 JS 强转）；`bool` 不提供任何转换（`str(true)`/`int(true)`/`float(true)` 均报错，"true" 字符串与 true 布尔是两种不同的类型）；唯一入口 `ScopeManager.validateType` + `evaluateOperation`/`evaluateUnaryOperation` 单点收紧，行解释器与 NSVM 双路径自动同步，`int`→`float` 标量宽松（`global x:float = 5`）与数组层三档严格均不受影响，doc.md 类型表/表达式章节同步说明；**内置函数参数显式类型校验**：`Math.*`/`Bit.*` 参数只收 `number`、`String.*` 的源串/目标串/替换串参数只收 `string`（`take` 的 `start`/`count`、`findFirst` 的 `from` 只收 `number`、`replace` 的 `all` 只收 `bool`）——**借用宿主 JS 能力但不继承其隐式强转**（此前 `Math.abs("-5")=5`、`String.take(12345,1,2)="23"`、`Math.max("3","7")=7`、`Bit.and("1",2)=1` 静默 JS 强转），类型不符抛 `TypeError` 可被 `try-catch` 捕获，需转换先显式 `int(x)`/`float(x)`/`str(x)`；统一走 `executeFunction` 单点分发（新增 `checkNumberArg`/`checkStringArg` 辅助，NSVM 表达式含内置函数本就整体回退树求值零影响）；**条件表达式必须布尔（truthy 拒绝）**：`while`/`for`/`assert`/`jump` 的非布尔条件一律报类型错误——此前行解释器 `while (5)`/`for` 条件经 JS `&&` 真值化静默通过（`5 && true` 得 `true` 绕过类型检查，`0` 等 falsy 又报错，行为不一致）、`assert (5)` 经 `if (!x)` 真值判断静默通过、`jump (5): tag` 静默跳转；现先类型检查再合并控制流状态（`while`/`for` 类型错误按假处理跳过整个循环体，避免顺序执行 + 循环回跳死循环），`executeAssert`/`executeJump` 补布尔检查，NSVM 的 JZ/ASSERTCHK 双路径同步（JZ 按语句类型判断：`while`/`for` 跳过循环体、`if` 顺序执行 if 体，与行解释器逐分支一致）；**`==`/`!=` 跨类型比较报错**：两侧类型必须相同，数字 vs 字符串 vs 布尔等跨类型比较一律报类型错误（可 try-catch 捕获）——此前类型不同返回 `false`（`5 != "a"` 为 `false` 不符合直觉）；数字类型间天然豁免（`int`/`float`/`number` 实现层同为数字，`5 == 5.0`、`0 == 0.0` 恒为 `true`）；**`&&`/`||` 有条件短路**：左侧为 `false`（`&&`）或 `true`（`||`）时右侧不求值（`false && 1/0` 不再触发除零，主流语言语义），两侧仍强制布尔；内置函数（`Math.*`/`String.*`/`Bit.*`/`len`/`str`/`int`/`float` 等）定位为"函数模样的运算符"，与运算符同级、无副作用，可自由出现在短路右侧——短路跳过它们不会跳过任何有意图的操作（用户函数只能通过 `call` 语句调用，天然不会出现在表达式中）；**I/O 型 `input()`** 不属于"函数模样的运算符"——与外界交互是它的目的（阻塞等待输入/挂起脚本）、有副作用，短路跳过它会"静默不问输入"，解析期检测短路右侧含 `input()` 即报语法错误可捕获，需输入请先 `if` 判断再显式调用；行解释器 `evalTree` 短路（左侧先布尔检查再决定是否求右）+ NSVM 表达式含 `&&`/`||` 整体回退树求值（线性字节码无条件跳转无法表达短路，双路径统一）；**String.\* 改名**：删除 JS 直译名 `String.length/substring/indexOf/includes`，改为语言自身风格 `String.take(s, start, count)`（取子串，边界全显式——`start`/`count` 非负、`start < len(s)`、`count <= len(s) - start`，越界抛 `RangeError` 可捕获，取到末尾写 `len(s) - start`；`count=0` 返空串）、`String.findFirst(s, target[, from])`（`from` 可省略，找不到返回 `false`，不再返回 `-1`——遵守"操作成功返回结果或 true，失败返回 false"语言规范）、`String.findLast(s, target)`（找不到返 `false`）、`String.has(s, target)`（包含判断返 bool）、`String.replace(s, old, new[, all])`（`all` 可省略默认替换第一处，`all=true` 替换所有出现）；字符串长度统一用内置 `len(s)`，删除 `String.length`；**移除 `String.split`** 定长容器填充（`call String.split` 独走 call 语句破坏一致性，findFirst 支持 `from` 后手写分割可行；删除 `executeBuiltinSplit` 快速通道与 `executeCall` 点分函数名正则，模块系统实现时再引入点分）；数组元素类型匹配收敛为**三档语义**——形参声明 `int` 只接 `int` 数组（`float` 数组拒收）；声明 `float` 只接 `float` 数组（`int` 数组拒收——否则 `mut` 形参接 `int` 数组实参后函数内写浮点值会被实参元素类型拒绝，出现"声明 float 却写不进浮点数"的怪异，三档让类型契约在调用点守住）；声明 `number` 兼容 `int`/`float` 元素但输出 `[WARN]` 提示"建议明确声明为 int 或 float"（与 `arrfill` 对 `number` 数组的警告一致）；`canArrayElementFit` 改为严格匹配 + 新增 `warnArrayNumberCompat`，行解释器与 NSVM 双路径 4 处校验点同步；标量层因 JS 值模型（`5` 与 `5.0` 同值，`isInteger(5.0)=true`）无法严格区分、且 `int`→`float` 在 double 下无损，维持宽松（"严格"只落在有写穿风险的数组层，见 doc.md 设计说明）；`tests/string_bit_tests.ns` 重写（新函数 + RangeError 可捕获 + 找不到返 false + 先 has 再 findFirst 标准写法），`tests/expr_ops_tests.ns` 新增测试 12（隐式转换禁止全覆盖），`tests/array_param_tests.ns` 新增测试 19（含字面量实参与警告路径），`tests/demo_test.ns` 的 `str(flag)` 改 if/else 输出（bool 不再可转换）；全部 25 测试逐字节回归一致，2048 确定性校验一致
  - 说明：**2.7 系列定位为不稳定系列**，patch 版本可携带破坏性变更（攒批策略——功能按 patch 随时发，破坏性变更同样按 patch 进 2.7.x，待 2.8 起恢复"破坏性变更只在 minor 升级时发布"）
- **2.7.1**：内置 String.* 与 Bit.* 对象（与 Math.* 同源，直接映射宿主 JS 能力，不依赖宿主注入）——`String.length/substring/indexOf/includes/replace/toUpper/toLower/trim` 8 个字符串函数（JS 语义：substring 负索引按 0、`end<start` 自动交换，replace 仅替换第一处）；`Bit.and/or/xor/not/shl/shr` 6 个位运算函数（32 位有符号语义，含高位溢出变负）；参数个数不符报类型错误（zh/en 双语模板，新增 `func_needs_3_args`）；仅改 `executeFunction` 分发器一处（含 `Math.*` 的表达式本就整体回退树求值，NSVM 表达式编译零影响）；新增 `tests/string_bit_tests.ns`（正常路径/边界/表达式嵌套/参数错误可捕获），doc.md 补 String/Bit 对象表；全部 25 测试逐字节回归一致
- **2.7.1** 追加：`String.split` 定长容器填充——语言无动态数组且表达式仅返回标量，split 不走表达式返回数组，改为 `call String.split(源串, 分隔符, mut 容器数组) -> 段数`（容器定长=容量上限，超容量抛 RangeError 可捕获；空分隔符按字符拆，连续分隔符保留空段）；`executeCall` 正则扩展支持点分函数名 + 内置函数快速通道（`executeBuiltinSplit`，与模块系统语句入口同向），NSVM 编译 CALL 正则不含点分自动整体回退行解释器零改动
- **2.7.0**：数组形参/返回值元素类型（破坏性语法变更）——函数数组形参必须声明元素类型：`arr[]:int`（只读引用）/ `mut arr[]:int`（可变引用，`[]` 表示不定长数组）；数组返回值同：`-> st[]:int`；旧 `arr:array` / `-> st:array` 语法解析期报语法错误。函数边界补齐元素类型校验（与标量 validateType 语义一致：`int` 严格、`float`/`number` 接受任意数字、`string`/`bool` 严格相等），行解释器 `executeCall` 与 NSVM `executeCallCompiled` 双路径同步，新增 `array_elem_type_mismatch` 报错；数组字面量实参数字元素改推断 `int`/`float`（整数→`int`，小数→`float`，与语言显式区分一致）；全部 24 测试逐字节回归一致，2048 确定性校验逐字节一致
- **2.6.5**：全局变量读取优化——evalTree 增加全局槽位快速路径,`GLOBAL_VARS` 对象属性 O(1) 直读,免 `getVariableInfo` 函数调用(原全局变量每读一次都走整条查找路径,是全局比局部槽位慢 ~40% 的主因,而非属性访问本身)；`getVariableInfo` 的 `hasOwnProperty` 改为直接访问(语义等价)；BISECT 实测 2048 端到端稳定 -1.4%、全局数组 len 热循环 -2.5%；23/23 测试逐字节回归一致,2048 确定性校验逐字节一致
- **2.6.4**：规范语义修正——字符串字面量收紧为仅双引号（tokenize/表达式求值/数组字面量实参/数组元素拆分统一,单引号不再是字符串边界,符合语言规范）；`--debug N` 命令行级别最高,显式指定后覆盖脚本内 debug 指令（恢复设计本意）；len(数组) 编译期折叠经实测否决——数组整体赋值会同步 `arrayLength`（引用共享语义),折叠为声明长度在 `a = b` 后语义错误,而免分发的运行时内联实测收益≈0（开销在变量读取),故保持运行时求值,doc 如实描述；全部 23 测试逐字节回归一致,2048 确定性校验逐字节一致
- **2.6.3**：debugLog 全量 i18n——260 处调试输出从中文写死迁移到 zh/en 语言包（ScopeManager/行解释器/表达式求值/NSVM 编译器与执行器 5 区域,`dbg_` 前缀词条分组管理），`--lang en` 下调试信息全部英文；zh 输出与迁移前逐字节一致,惰性闭包零性能影响；全部 23 测试逐字节回归一致,2048 确定性校验逐字节一致
- **2.6.2**：函数调用热路径优化——CALLFUNC 元数据编译期预解析函数体首条可执行行（免运行期逐行扫描 `programLines`）、RETV/RET 直调执行器（免 `executeCommand` switch 二次分发）、函数调用路径 debugLog 系统惰性化（低级别免闭包创建,高级别输出逐字节不变）；函数调用合成基准 -8%（9228x→8420x vs JS）,2048 端到端 -4.1%；全部测试逐字节回归一致
- **2.6.0**：数组指令化（阶段3 深化）——数组声明与赋值按 v0.2 设计稿 §5.7 真正指令化：`NEWARRAY`/`SETARRAY` 指令 + 常量池数组元数据（声明格式正则、元素拆分、初始化值解析全部前移到编译期预解析，失败整体回退行解释器），`executeArrayDeclarationCompiled`/`executeArrayAssignmentCompiled` 外提执行器逐字节复刻调试输出与错误消息（含数组越界/长度不匹配/arrfill 填充）；`GETARRAY` 机制保留但不启用（BISECT 实测完整实现 2048 端到端 +6~8%）；编译期预解析置静默标志防止污染运行期调试输出；`wrapEvalError` 折叠回 `evaluate` 内联（外提会缩小 evaluate 体积触发 Turbofan 将其内联进 NSVMExecutor.run 的 OSR 图，使冷启动编译耗时从 ~13ms 增至 ~18ms）；相对 2a7b98c 数组指令化净收益 -0.8%（官方基准相对 HEAD~1 的 +1.5% 经溯源来自已提交的 CALLFUNC 差异）；全部 24 测试逐字节回归一致，2048 确定性校验逐字节一致
- **2.5.1**：CALLFUNC 指令化（阶段3 深化）——用户函数调用按 v0.2 设计稿 §5.6 真正指令化：删除 ARG 搬运，`modesK` 常量池实参模式表（长度即实参个数，`argc` 绝不从操作数字段携带），实参解析/模式判定/个数与返回值规则校验全部前移到编译期（任一不满足整体回退行解释器），`executeCall` 完整调试输出逐字节复刻（含 `执行指令`/`函数信息`/`当前局部变量详情`/`检查参数`/`详细检查参数`/`函数体开始行`/`变量作用域详情`/`当前流程控制栈`）；全部 23 测试逐字节回归一致，2048 确定性校验逐字节一致，函数调用合成基准 -9.2%
- **2.5.0**：寄存器型 NSVM 指令集（阶段3 全覆盖）——控制流（if/while/for/switch/jump/break/continue）指令化并逐字节复刻行号与调试输出、函数调用帧切换（CALL/RETV/RET，返回失败路径复刻"报错后继续执行函数体"）、try-catch 异常处理器表 + 跨函数冒泡、ASSERT/PURGE 等语句指令化；编译失败整体回退行解释器；全部 23 个测试逐字节回归一致，2048 端到端基准 -17.5%
- **2.4.4**：性能优化——局部变量槽位化 O(1) 访问、函数调用特化（参数/返回值帧槽位、帧清理 O(1) 截断）、表达式求值数字快速路径、数组访问帧缓存；新增 `benchmarks/` 基准测试（合成基准 + 2048 端到端 + 确定性校验），基准自动打印运行环境，README 补历史性能记录追溯
- **2.4.0**：内置 `input()` 运行时输入——命令行同步读取 stdin（含中文），浏览器默认 prompt 弹窗、可 `NSI.setInput()` 自定义绑定（如页面输入框）；新增浏览器交互执行 `NSI.runInteractive`/`NSI.resumeInput`（input() 挂起/恢复，脚本可自持主循环，双环境通用）
- **2.3.1**：浏览器接口——全局 `window.NSI`（`run`/`setLanguage`/`getLanguage`/底层类），浏览器可直接加载 `dist` 产物运行脚本
- **2.3.0**：完整中英国际化（i18n）——`--lang en|zh` 切换输出语言，158 对双语消息模板，错误类型名中英对照，第一行切换提示
- **2.2.0**：数组四种传参模式、数组整体赋值与 `copy` 深拷贝、`return` 规则检测、只读传播修复
- **2.1.0**：声明初始化字面量限制、关键字补全、文档同步
